import { PageSubHeader } from '@/components'

export default function Loading() {
	return (
		<div className='flex items-center justify-center'>
			<PageSubHeader headerContent='Trwa ładowanie danych...' />
		</div>
	)
}
